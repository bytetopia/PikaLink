version: '3.8'

services:
  pikalink:
    image: bytetopia/pikalink:latest
    ports:
      - "8080:8080"
    volumes:
      - /YOUR_OWN_PATH_ON_HOST_MACHINE/:/app/data
    environment:
      - DATA_PATH=/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/admin/"]
      interval: 120s
      timeout: 10s
      retries: 3
      start_period: 40s
